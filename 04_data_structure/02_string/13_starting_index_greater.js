let s = "Learning JavaScript";
let res = s.substring(13, 8);
// If the starting index is greater than the ending index, the substring() method swaps the values of the indices internally. 
// This makes it easy to extract the substring regardless of the order of indices.
console.log(res);
