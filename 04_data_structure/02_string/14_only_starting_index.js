let s = "JavaScript is amazing!";
let res = s.substring(11);
// If only the starting index is provided, 
// substring() will return the substring from that index to the end of the string.
console.log(res);
